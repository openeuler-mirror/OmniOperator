#!/usr/bin/env sh
# help: get a tty inside the toolbox container

OMNITOOLS_DIR="${OMNITOOLS_DIR:-../../}"

main() {
  # . "${OMNITOOLS_DIR}/cmds/docker/run.sh"
  # FLAG_cmd=bash FLAG_tag="$OMNITOOLS_IMAGE" main
  bash
}
